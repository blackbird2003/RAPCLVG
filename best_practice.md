Q: 当我在一个项目中，需要一边与codex讨论研究idea,一边要求推进细节较多的系统工程实现时，为了稳健、清晰地推进并节省token,比较好的实践方式是什么


A: 比较稳健的方式是：把“研究思考”和“工程执行”分成两条线程，用少量项目文档作为二者之间的接口，而不是全部塞在一个越来越长的对话里。

```text
研究 / 架构线程
    ↓ 更新决策与验收标准
DECISIONS.md + PLAN.md + AGENTS.md
    ↓ 读取
工程实现线程（Worktree / Goal）
    ↓
测试、diff、里程碑结果
    ↑
研究线程评估与调整方向
```

### 推荐配置

1. 研究 / 架构线程

负责：

- 探索 idea、挑战假设
- 比较方案和实验设计
- 明确接口、约束、指标
- 决定“做什么”，不承担大量编码和日志

建议使用 Plan 模式。每轮讨论结束，不要保留大篇聊天总结，而是只更新：

- `DECISIONS.md`：已作出的决定、理由、仍未解决的问题
- `PLAN.md`：当前里程碑、任务依赖、验收标准
- 必要时用 `ADR/xxx.md` 保存真正重要的架构决策

给这个线程的常用指令可以是：

```text
先讨论和质疑这个 idea，不修改实现。

本轮结束时，只输出：
1. 新增或改变的决定
2. 未解决问题
3. 对当前工程计划的增量修改
4. 下一项最便宜的验证实验
```

这会显著减少“每轮重新总结整个世界”的 token 消耗。

2. 工程实现线程

每个相对独立的里程碑开一个线程，复杂任务使用 Goal mode，实验性或并行工作使用 Worktree。

典型提示：

```text
目标：
实现 PLAN.md 中的 M2。

上下文：
- PLAN.md
- DECISIONS.md
- src/runtime/
- tests/runtime/

约束：
- 不改变 DECISIONS.md 中已冻结的接口
- 不引入新的生产依赖
- 遇到研究层面的歧义时停止并列出具体问题

完成条件：
- 指定测试通过
- 新行为有测试覆盖
- 检查 diff
- 最后只汇报改动、验证结果、风险和待决策项
```

官方推荐的提示结构也是 `Goal / Context / Constraints / Done when`，非常适合作为固定模板。[Codex best practices](https://developers.openai.com/codex/learn/best-practices)

### 三类信息要放在不同位置

- `AGENTS.md`：长期稳定的仓库规则，例如目录结构、测试命令、编码约定、禁止事项。
- `DECISIONS.md`：当前研究过程中不断演化的结论。
- 当前 prompt：仅本次任务的目标、范围和临时约束。

不要把研究笔记和某个实验的临时结论不断追加到 `AGENTS.md`。它会自动进入后续上下文，太长既费 token，又容易让过期结论获得过高权重。官方也建议保持 `AGENTS.md` 短小、准确、实用。[AGENTS.md guidance](https://developers.openai.com/codex/guides/agents-md)

### 一个实用节奏

以一个里程碑为循环：

1. 研究线程提出假设和最小验证实验。
2. 写入 `DECISIONS.md` 与 `PLAN.md`。
3. 工程线程在 Worktree 中实现并验证。
4. 工程线程只返回：
   - 实际结果
   - 与预期的差异
   - 测试证据
   - 需要研究判断的问题
5. 研究线程据此更新方向。
6. 达到里程碑后提交；下一里程碑开新工程线程。

这样，研究线程保留“为什么”，工程线程保留“怎么做”，Git 保留“具体改了什么”。

### 节省 token 的关键习惯

- 用文件路径引用上下文，不要反复粘贴代码和历史讨论。
- 要求“只报告增量变化”，不要每轮生成完整总结。
- 要求测试日志只保留失败摘要和关键错误，不返回完整输出。
- 一个工程线程只负责一个可验收里程碑。
- 常规实现用较低或中等 reasoning；架构、复杂调试再提高。
- 对长任务使用 Goal mode保存明确目标，但不要把所有项目工作变成一个永久线程。[Goal mode](https://developers.openai.com/codex/prompting#goal-mode)
- 两个线程不要同时修改相同文件；并行写操作用不同 Worktree。[Worktrees](https://developers.openai.com/codex/app/worktrees)
- 子代理适合代码搜索、测试、日志分析等只读且独立的任务；它节省的是时间，通常不节省总 token。

最值得记住的一句话是：

> 聊天用于探索，文件用于定案，Goal 用于交付，Git 与测试用于证明。

对多数研究型工程项目，我会选择“一个长期研究线程 + 每个里程碑一个短工程线程 + `DECISIONS.md`/`PLAN.md` 作为协议”。这是清晰度、连续性和 token 成本之间比较舒服的平衡点。